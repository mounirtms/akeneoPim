<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome to Pimcore</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <div class="row">
            <div class="col-md-12 text-center">
                <h1>Welcome to Pimcore</h1>
                <p class="lead">Your Pimcore installation is working correctly!</p>
                {% if app.environment == 'dev' %}
                    <div class="alert alert-info">
                        <p>You are currently in development mode.</p>
                        <p>To access the admin panel, go to <a href="/admin">/admin</a></p>
                    </div>
                {% endif %}
                
                <div class="mt-4">
                    <a href="/admin" class="btn btn-primary">Access Admin Panel</a>
                </div>
            </div>
        </div>
    </div>
</body>
</html>