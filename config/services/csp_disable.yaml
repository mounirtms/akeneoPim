# Disable CSP completely - allow all inline scripts for RequireJS
services:
    _defaults:
        autowire: true
        autoconfigure: true

    # High-priority listener to remove CSP headers after they're set
    AppBundle\EventListener\RemoveCspListener:
        autowire: true
        autoconfigure: true
        tags:
            - { name: kernel.event_subscriber }
