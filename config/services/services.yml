parameters:
    pim_ce_dev_src_folder_location: '%kernel.project_dir%/vendor/akeneo/pim-community-dev'

imports:
    - { resource: '../../src/AppBundle/Resources/config/services.yml' }

services:
    # Default services from Akeneo