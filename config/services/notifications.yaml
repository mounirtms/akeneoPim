# Email notification event subscribers
# These are auto-wired and auto-configured via config/services.yaml (App\ namespace)
# No explicit service definitions needed - Symfony autoconfigure registers them automatically
services: {}
